# 스폰지클럽 — AI 스터디 & 커뮤니티

> 매주 만들고, 각자 나눈다.

AI 스터디·커뮤니티 Obsidian Vault. **6개조 × 12~13명 (총 75명)**이 매주 과제를 제출하고, 각자 인사이트를 공유한다. AI가 분석·제안·배포를 자동화한다.

> **사람이 처음 보는 진입점은 `00_START.md`** (볼트 전체 구조를 5분 안에 파악). 이 `CLAUDE.md`는 AI 세션용 상세 규칙이다.

---

## 레포지토리 구조

| 레포 | 역할 | 배포 |
|------|------|------|
| `spongeclub` (이 레포) | Obsidian Vault — 콘텐츠 원본 | - |
| _TBD_ | 공식 홈페이지 | Vercel |

> 사이트 레포 정보 입력 대기 중. 결정 후 `/publish` 명령어 신규 작성 + 이 표 갱신.

### 데이터 흐름 (예정)

```
Obsidian Vault (여기서 작성)
       ↓ sync-content (공개 폴더만 복사)
사이트 레포 (빌드)
       ↓ git push
Vercel 자동 배포
```

---

## 폴더 구조

### 멤버가 쓰는 폴더 (5개 뼈대)

| 폴더 | 설명 | 홈페이지 반영 |
|------|------|-------------|
| `00_notice/` | 운영 공지 | (미정) |
| `01_meeting/` | 주차별 회의록 (`weekly_0T/`, `weekly_1주차/` ~ `weekly_6주차/`) | 비공개 |
| `02_mission/` | 주차별 과제 (`1조/` ~ `6조/`) | `/archive/` |
| `03_insights/` | 좋은 정보·인사이트 공유 | `/insights/` |
| `04_etc/` | 기타 자료 | (선택) |

### 운영자 전용 폴더

| 폴더 | 설명 |
|------|------|
| `99_meta/` | 멤버목록, 운영 가이드 등 (`99_` prefix로 사이드바 맨 아래) |
| `.claude/commands/` | 슬래시 명령어 |

> AAA 시절 사용했던 `06_sharing/`, `90_analysis/`, `91_proposals/`, `92_status/`, `99_templates/`는 현재 사용 안 함. 필요해지면 추가.

---

## 슬래시 명령어

| 명령어 | 기능 | 누가 |
|--------|------|------|
| `/mission N [주제]` | 멤버 과제 노트 작성/보강 (초안 있으면 구조화) | 멤버 |
| `/analyze N` | N주차 통합 분석 + 스킬 제안/인사이트 갱신 | 운영자 |
| `/ln N` | N주차 멤버별 링크드인 초안 | 운영자 |
| `/retag` | 인사이트 키워드 frontmatter 재정리 | 운영자 |

> 명령어 본문은 아직 새 폴더 뼈대로 미동기화 (`00_missions/` 등 옛 경로 참조 중). 다다 검토 후 개별 수정 예정. `/publish`는 사이트 레포 결정 후 신규 작성.

### 운영자 작업 흐름 (예정)

```bash
/analyze 3 → /ln 3 → /publish   # publish는 사이트 레포 결정 후
```

---

## 파일명 규칙

- **미션**: `1주차_닉네임_submit.md` (위치: `02_mission/조N/`)
- **회의록**: `01_meeting/weekly_N주차/` 안에 자유 작성 (예: `weekly_3주차_조4.md`)
- **인사이트**: `03_insights/` 안에 자유 (`닉네임_제목.md` 권장)
- **나머지**: 자유 (`_` 또는 `-` 사용, 특수문자 금지)

## 커밋 메시지 규칙

```
[프리픽스] 변경 내용
```

| 프리픽스 | 용도 |
|---------|------|
| `[mission]` | 주차별 과제 제출 |
| `[insight]` | 인사이트·스킬 공유 |
| `[analysis]` | 분석 리포트 |
| `[meeting]` | 회의록 |
| `[notice]` | 운영 공지 |
| `[structure]` | 폴더/파일 구조 변경 |
| `[command]` | 슬래시 명령어 변경 |
| `[meta]` | CLAUDE.md, MEMORY.md, 운영 문서 |
| `[site]` | 사이트 레포/배포 관련 (사이트 생성 후) |

## 참여자

- **규모**: 6개조 × 12~13명 (총 75명)
- **명단**: `99_meta/멤버목록.md` (조 단위, 이름만 기록)
- 멤버는 본인 조와 닉네임을 알아야 `/mission` 명령어로 정확한 경로에 파일 생성 가능

## 홈페이지 & 배포

| 용도 | URL |
|------|-----|
| 홈페이지 | _TBD (Vercel 배포 예정)_ |

---

## 팀 메모리 운영 규칙

### MEMORY.md란
`MEMORY.md`는 팀 전체가 공유하는 맥락 기록이다. 모든 Claude Code 세션 시작 시 자동으로 읽히며, 대화 종료 시 갱신 여부를 체크한다.

### 기록하는 것
- 의사결정: "A 대신 B를 선택했다. 이유는 ~"
- 구조 변경: 폴더 추가/이동, 파일 규칙 변경
- 새로 알게 된 제약사항: API 한계, 플랫폼 제약
- 중요한 버그/이슈 해결: 다른 사람도 만날 수 있는 것

### 기록하지 않는 것
- 단순 파일 편집 (회의록 정리, 오타 수정)
- 코드에서 바로 확인 가능한 것
- git log로 알 수 있는 것

### 갱신 방법
1. `최근 변경 로그` 맨 위에 한 줄 추가: `- [YYYY-MM-DD] 닉네임 — 변경 내용`
2. 의사결정이 있었다면 `활성 의사결정` 섹션도 갱신
3. 항목이 15건을 넘으면 오래된 것을 `99_meta/memory-archive.md`로 이동

### Stop 훅
대화 종료 시 파일 변경이 있었는데 MEMORY.md가 갱신되지 않았으면 리마인드가 뜬다.
- 기록할 내용이 있으면 → MEMORY.md 갱신
- 기록할 내용이 없으면 → `touch .claude/.memory-checked` 실행 후 종료
