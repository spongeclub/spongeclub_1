# 스폰지클럽 — AI 스터디 & 커뮤니티

> 매주 만들고, 각자 나눈다.

AI 스터디·커뮤니티 Obsidian Vault. **6개조 × 12명 = 72명**이 매주 과제를 제출하고, 각자 인사이트를 공유한다. AI가 분석·제안·배포를 자동화한다.

> **사람이 처음 보는 진입점은 `00_START.md`** (볼트 전체 구조를 5분 안에 파악). 이 `CLAUDE.md`는 AI 세션용 상세 규칙이다.

---

## 레포지토리 구조

| 레포 | 역할 | 배포 |
|------|------|------|
| `spongeclub_1` (이 레포) | Obsidian Vault — 콘텐츠 원본 **+ 사이트 코드(`_site/`)** | Vercel (root: `_site`) |

> ⭐ **사이트 소스 = `_site/` 단일.** (2026-06-01 통합)
> 예전 별도 레포 `spongeclub/spongeclub_homepage`는 `_site/`로 통합·은퇴했다 — 더 이상 수정하지 않는다.
> - 사이트 디자인/기능 수정 → `_site/src/` (자세한 건 `_site/README.md`)
> - 콘텐츠 수정 → 아래 vault 본체 폴더
> - Vercel은 vault `main` 머지 시 `_site/`를 빌드해 자동 배포.

### 데이터 흐름 (예정)

```
Obsidian Vault (여기서 작성)
       ↓ sync-content (공개 폴더만 복사)
사이트 레포 (빌드)
       ↓ git push
Vercel 자동 배포
```

---

## 폴더 구조

### 멤버가 쓰는 폴더

| 폴더 | 설명 | 홈페이지 반영 |
|------|------|-------------|
| `00_missions/` | 주차별 과제 (조 → 멤버 단위) | `/archive/` |
| `02_skill&insight/` | 인사이트·스킬 공유 | `/insights/` |
| `01_meetings/` | 주차별 회의록 (조별/전체) | 비공개 |
| `06_sharing/` | 발표·공유 자료 | 비공개 |

### AI 자동 생성 폴더

| 폴더 | 설명 | 홈페이지 반영 |
|------|------|-------------|
| `90_analysis/weekly/` | AI 주차 분석 리포트 | `/analysis/` |
| `90_analysis/members/` | 멤버 프로필 (`조N_닉네임_프로필.md`) | 비공개 |
| `90_analysis/teams/` | 조별 분석 (선택) | 비공개 |
| `91_proposals/` | 스킬 제안 + 운영 인사이트 | `/proposals/` |
| `92_status/` | 출석·과제 현황 (조 단위 집계) | 비공개 |

### 운영

| 폴더 | 설명 |
|------|------|
| `99_meta/` | 운영 가이드, 조-멤버목록, 파일명규칙, 시스템가이드 |
| `99_templates/` | Obsidian 템플릿 |
| `.claude/commands/` | 슬래시 명령어 |

---

## 슬래시 명령어

| 명령어 | 기능 | 누가 |
|--------|------|------|
| `/mission N [주제]` | 멤버 과제 노트 작성/보강 (초안 있으면 구조화) | 멤버 |
| `/submit` | 미션 노트 제출 — 브랜치·커밋·푸쉬·PR·스쿼시 머지 자동 ("미션 제출할게"도 동작) | 멤버 |
| `/analyze N` | N주차 통합 분석 + 스킬 제안/인사이트 갱신 | 운영자 |
| `/ln N` | N주차 멤버별 링크드인 초안 | 운영자 |
| `/retag` | 인사이트 키워드 frontmatter 재정리 | 운영자 |

> `/publish`는 사이트 레포 결정 후 신규 작성 예정.

### 운영자 작업 흐름 (예정)

```bash
/analyze 3 → /ln 3 → /publish   # publish는 사이트 레포 결정 후
```

---

## 파일명 규칙

- **미션**: `Week_0N_조N_닉네임_submit.md` (위치: `00_missions/Week_0N_submit/조N_닉네임/`)
- **멤버 프로필**: `90_analysis/members/조N_닉네임_프로필.md`
- **회의록**: `01_meetings/Week_0N_weekly.md` 또는 `01_meetings/Week_0N_조N.md`
- **나머지**: 자유 (`_` 또는 `-` 사용, 특수문자 금지)

## 커밋 메시지 규칙

```
[프리픽스] 변경 내용
```

| 프리픽스 | 용도 |
|---------|------|
| `[mission]` | 주차별 과제 제출 |
| `[insight]` | 인사이트·스킬 공유 |
| `[analysis]` | 분석 리포트 |
| `[meeting]` | 회의록 |
| `[structure]` | 폴더/파일 구조 변경 |
| `[command]` | 슬래시 명령어 변경 |
| `[meta]` | CLAUDE.md, MEMORY.md, 운영 문서 |
| `[sharing]` | 발표·공유 자료 |
| `[site]` | 사이트 레포/배포 관련 (사이트 생성 후) |

## 제출 워크플로우 (필수)

> **main 브랜치에 직접 push 금지.** 모든 변경은 브랜치 → PR → 머지 흐름을 따른다.

멤버가 파일 제출·수정·공유를 요청하면 아래 순서로 처리한다:

1. `git pull origin main` (최신화)
2. `git checkout -b submit/조N-닉네임` (브랜치 생성)
3. 파일 추가/수정 후 `git add` → `git commit`
4. `git push -u origin submit/조N-닉네임`
5. `gh pr create --title "[프리픽스] 조N 닉네임 - 내용" --body ""`
6. `gh pr merge --squash` (즉시 스쿼시 머지)
7. `git checkout main && git pull origin main` (정리)

### 브랜치 이름 규칙
- `submit/조N-닉네임` (예: `submit/3조-흐민`)
- 동일 브랜치가 이미 있으면 `submit/조N-닉네임-N주차` 사용

### 멤버 식별
- 멤버가 "나 3조 흐민이야"라고 하면 `99_meta/멤버목록.md`에서 확인 후 진행
- 조와 닉네임을 말하지 않으면 먼저 물어본다

---

## 참여자

- **규모**: 6개조 × 12명 = 72명
- **조 정보**: `99_meta/멤버목록.md`에 `조N | 닉네임 | GitHub ID | 역할`로 기록
- 멤버는 본인 조와 닉네임을 알아야 `/mission` 명령어로 정확한 경로에 파일 생성 가능

## 홈페이지 & 배포

| 용도 | URL |
|------|-----|
| 홈페이지 | _TBD (Vercel 배포 예정)_ |

---

## 팀 메모리 운영 규칙

### MEMORY.md란
`MEMORY.md`는 팀 전체가 공유하는 맥락 기록이다. 모든 Claude Code 세션 시작 시 자동으로 읽히며, 대화 종료 시 갱신 여부를 체크한다.

### 기록하는 것
- 의사결정: "A 대신 B를 선택했다. 이유는 ~"
- 구조 변경: 폴더 추가/이동, 파일 규칙 변경
- 새로 알게 된 제약사항: API 한계, 플랫폼 제약
- 중요한 버그/이슈 해결: 다른 사람도 만날 수 있는 것

### 기록하지 않는 것
- 단순 파일 편집 (회의록 정리, 오타 수정)
- 코드에서 바로 확인 가능한 것
- git log로 알 수 있는 것

### 갱신 방법
1. `최근 변경 로그` 맨 위에 한 줄 추가: `- [YYYY-MM-DD] 닉네임 — 변경 내용`
2. 의사결정이 있었다면 `활성 의사결정` 섹션도 갱신
3. 항목이 15건을 넘으면 오래된 것을 `99_meta/memory-archive.md`로 이동

### Stop 훅
대화 종료 시 파일 변경이 있었는데 MEMORY.md가 갱신되지 않았으면 리마인드가 뜬다.
- 기록할 내용이 있으면 → MEMORY.md 갱신
- 기록할 내용이 없으면 → `touch .claude/.memory-checked` 실행 후 종료
