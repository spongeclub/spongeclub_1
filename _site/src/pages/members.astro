---
import Layout from '../layouts/Layout.astro';
import { loadMembers } from '../lib/members';

const members = loadMembers();
const teamCount = new Set(members.map((m) => m.team)).size;
const LOGO = '/assets/sponge-logo.png';

// 조별 점 색 (조 구분용)
const TEAM_COLORS: Record<string, string> = {
  '1조': 'oklch(68% 0.20 25)',   // 빨강
  '2조': 'oklch(74% 0.17 60)',   // 주황
  '3조': 'oklch(80% 0.16 110)',  // 노랑연두
  '4조': 'oklch(68% 0.16 155)',  // 초록
  '5조': 'oklch(66% 0.17 245)',  // 파랑
  '6조': 'oklch(60% 0.18 300)',  // 보라
};

// 조별 그룹핑 (members는 이미 조 순서로 정렬됨)
const teamGroups: { team: string; list: typeof members }[] = [];
for (const m of members) {
  const last = teamGroups[teamGroups.length - 1];
  if (last && last.team === m.team) last.list.push(m);
  else teamGroups.push({ team: m.team, list: [m] });
}
---

<Layout title="스폰지클럽 1기 — 스폰지클럽" bodyClass="sc-page">
  <header class="sc-header">
    <div class="sc-header-inner">
      <a href="/" class="sc-brand">
        <span class="sc-brand-mark">🧽</span>
        <span class="sc-brand-name">spongeclub</span>
        <span class="sc-brand-sep">/</span>
        <span class="sc-brand-sub">스폰지클럽 1기</span>
      </a>
      <nav class="sc-nav" aria-label="메인 메뉴">
        <a href="/skills/">스킬&인사이트</a>
        <a href="/gallery/">갤러리</a>
        <a href="/members/" class="active">크루</a>
        <a href="/#archive">주차별 아카이브</a>
      </nav>
    </div>
  </header>

  <main class="sc-main">
    <section class="sc-title-section">
      <p class="sc-kicker">함께 만드는 사람들</p>
      <h1 class="sc-h1">
        <span class="sc-shape sc-shape--circle" aria-hidden="true"></span>
        스폰지클럽 1기
      </h1>
      <p class="sc-lede">
        매주 만들고 나누는 <span class="sc-highlight">{members.length}명</span>의 빌더들.
        카드를 눌러 뒤집으면 소개·SNS·산출물을 볼 수 있어요.
      </p>
    </section>

    {teamGroups.map(({ team, list }) => (
    <section class="mm-team-section" style={`--team-color: ${TEAM_COLORS[team] ?? 'var(--sc-accent-yellow)'};`}>
      <div class="mm-team-label"><span class="mm-team-dot" aria-hidden="true"></span>{team}</div>
      <div class="mm-grid">
      {list.map((m) => (
        <button class="mm-card" type="button" aria-label={`${m.nickname} 카드 뒤집기`}>
          <div class="mm-inner">
            <!-- 앞면 -->
            <div class="mm-front">
              <div class="mm-avatar">
                <img src={m.image || LOGO} alt={m.nickname} loading="lazy" class={m.image ? '' : 'is-logo'} />
              </div>
              <div class="mm-name">{m.nickname}</div>
              <div class="mm-team">{m.team}{m.isLeader && <span class="mm-crew">조장</span>}</div>
            </div>
            <!-- 뒷면 -->
            <div class="mm-back">
              <div class="mm-back-name">{m.nickname}</div>
              {m.oneliner && <p class="mm-oneliner">{m.oneliner}</p>}
              {(m.sns.instagram || m.sns.linkedin || m.sns.threads) && (
                <div class="mm-sns">
                  {m.sns.instagram && <a href={m.sns.instagram} target="_blank" rel="noopener noreferrer" onclick="event.stopPropagation()"><img src="/assets/sns/instagram.svg" alt="" class="mm-sns-icon" />Instagram</a>}
                  {m.sns.linkedin && <a href={m.sns.linkedin} target="_blank" rel="noopener noreferrer" onclick="event.stopPropagation()"><img src="/assets/sns/linkedin.svg" alt="" class="mm-sns-icon" />LinkedIn</a>}
                  {m.sns.threads && <a href={m.sns.threads} target="_blank" rel="noopener noreferrer" onclick="event.stopPropagation()"><img src="/assets/sns/threads.png" alt="" class="mm-sns-icon" />Threads</a>}
                </div>
              )}
              {m.gallery.length > 0 && (
                <div class="mm-works">
                  {m.gallery.map((g) => (
                    <a href={g.url} target="_blank" rel="noopener noreferrer" onclick="event.stopPropagation()" class="mm-work">{g.title} ↗</a>
                  ))}
                </div>
              )}
              {!m.oneliner && !m.sns.instagram && !m.sns.linkedin && !m.sns.threads && m.gallery.length === 0 && (
                <p class="mm-empty">소개 준비 중</p>
              )}
            </div>
          </div>
        </button>
      ))}
      </div>
    </section>
    ))}

    <a href="/" class="sc-back">← 홈으로</a>
  </main>

  <footer class="sc-footer">
    스폰지클럽 · 구성원 {members.length}명 · {teamCount}개 조
  </footer>
</Layout>

<script is:inline>
  document.querySelectorAll('.mm-card').forEach((card) => {
    card.addEventListener('click', () => card.classList.toggle('is-flipped'));
  });
</script>

<style is:global>
  /* 조별 영역 */
  .mm-team-section {
    background: #e8f3fd; /* 연한 하늘색 */
    border: 1px solid #bcd9f5; /* 푸른 계열 경계 */
    border-radius: var(--sc-radius-card);
    padding: 18px 18px 22px;
    margin-bottom: 22px;
  }
  .mm-team-label {
    display: inline-flex; align-items: center; gap: 7px;
    font-size: 12px; font-weight: 800; letter-spacing: 0.06em;
    color: var(--sc-text-muted-high);
    margin-bottom: 14px;
  }
  .mm-team-dot {
    width: 10px; height: 10px; border-radius: 50%;
    background: var(--team-color, var(--sc-accent-yellow));
  }

  .mm-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(170px, 1fr));
    gap: 14px;
  }

  .mm-card {
    appearance: none;
    border: none;
    background: transparent;
    padding: 0;
    cursor: pointer;
    perspective: 1000px;
    aspect-ratio: 3 / 4;
    font-family: inherit;
  }

  .mm-inner {
    position: relative;
    width: 100%;
    height: 100%;
    transition: transform 480ms cubic-bezier(.2,.8,.3,1.1);
    transform-style: preserve-3d;
  }
  .mm-card.is-flipped .mm-inner { transform: rotateY(180deg); }

  .mm-front, .mm-back {
    position: absolute;
    inset: 0;
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
    border: 1px solid #cfe3f7; /* 푸른 계열 경계 */
    border-radius: var(--sc-radius-card);
    background: #ffffff;
    box-shadow: 0 2px 10px rgba(26, 31, 54, 0.07);
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }

  /* 앞면 */
  .mm-front {
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 20px 16px;
    gap: 2px;
  }
  .mm-avatar {
    width: 88px; height: 88px;
    border-radius: 50%;
    background: #fff1c4; /* 연노랑 — 로고와 조화 */
    display: flex; align-items: center; justify-content: center;
    overflow: hidden;
    margin-bottom: 12px;
  }
  .mm-avatar img { width: 100%; height: 100%; object-fit: cover; }
  .mm-avatar img.is-logo { object-fit: contain; padding: 14px; }
  .mm-name {
    font-family: var(--sc-font-display);
    font-weight: 400;
    font-size: 20px;
    color: var(--sc-text-primary);
    line-height: 1.15;
  }
  .mm-fullname { font-size: 12px; color: var(--sc-text-muted-mid); }
  .mm-team {
    margin-top: 6px;
    font-size: 11px; font-weight: 700;
    color: var(--sc-text-muted-high);
    display: inline-flex; align-items: center; gap: 6px;
  }
  .mm-crew {
    font-size: 10px; font-weight: 700;
    background: var(--sc-accent-blue); color: #fff;
    padding: 1px 6px; border-radius: 6px;
  }

  /* 뒷면 */
  .mm-back {
    transform: rotateY(180deg);
    padding: 18px 16px;
    gap: 10px;
  }
  .mm-back-fullname {
    font-size: 12px; color: var(--sc-text-muted-mid);
    margin-top: -4px;
  }
  .mm-back-name {
    font-family: var(--sc-font-display);
    font-weight: 400;
    font-size: 17px;
    color: var(--sc-text-primary);
  }
  .mm-oneliner {
    font-size: 13px; line-height: 1.55;
    color: var(--sc-text-primary);
    margin: 0;
  }
  .mm-sns { display: flex; flex-direction: column; gap: 6px; }
  .mm-sns a {
    display: inline-flex; align-items: center; gap: 7px;
    font-size: 12px; font-weight: 600;
    color: var(--sc-text-primary);
    text-decoration: none;
  }
  .mm-sns a:hover { color: var(--sc-accent-blue); }
  .mm-sns-icon {
    width: 16px; height: 16px;
    object-fit: contain;
    flex-shrink: 0;
  }
  .mm-works { display: flex; flex-direction: column; gap: 5px; margin-top: auto; }
  .mm-work {
    font-size: 11.5px; font-weight: 600;
    color: var(--sc-text-primary);
    background: var(--sc-surface-white);
    border: 1px solid var(--sc-border-card);
    border-radius: 8px;
    padding: 5px 9px;
    text-decoration: none;
  }
  .mm-work:hover { border-color: var(--sc-text-primary); }
  .mm-empty {
    font-size: 12px; color: var(--sc-text-muted-mid);
    margin: auto 0; text-align: center;
  }

  @media (max-width: 640px) {
    .mm-grid { grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); }
  }
</style>
